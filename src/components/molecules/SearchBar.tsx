import Button from "../atoms/Button";
import Input from "../atoms/Input";

const SearchBar = () => {
   return (
      <div>
         <Input />
         <Button />
      </div>
   );
};

export default SearchBar;
